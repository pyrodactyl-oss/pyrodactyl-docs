---
title: Installing Elytra (Native)
description: How to install and setup Elytra on Linux
---

<Callout type="error">
  Elytra is **no longer maintained**. This guide is kept for archival purposes.
  We recommend using [Pterodactyl Wings](https://pterodactyl.io/wings/1.0/installing.html) instead.
</Callout>
<Callout type="info">
  This guide covers installing Elytra natively on supported Linux distributions.
</Callout>

## Supported Operating Systems

<Callout type='info'>
  There is a very high chance it will work on more than these. These are the ones that are known working and officially supported.
</Callout>

| OS      | Version       |
|---------|---------------|
| Fedora  | 40–42         |
| Ubuntu  | 24.04         |
| Debian  | 13            |
| Rocky   | 10            |
| RHEL    | 10            |
| Arch    | Rolling       |

## Prerequisites

Before installing, ensure you have the following:

- curl
- tar
- Docker Engine
- [Rustic](https://github.com/rustic-rs/rustic/releases/latest) (recommended for deduplicated backups)
- [Certbot](https://github.com/certbot/certbot) (only if using SSL)

---

<Steps>
<Step>
### Install Docker

<Icon name="docker" className="inline size-4 mr-1.5 text-blue-400" />
Select your OS below for the appropriate commands:

<Tabs items={["Fedora", "Ubuntu", "Debian", "Rocky", "RHEL", "Arch"]}>
<Tab value="Fedora">
```bash
sudo dnf update
sudo dnf install curl tar
sudo dnf -y install dnf-plugins-core
sudo dnf-3 config-manager --add-repo https://download.docker.com/linux/fedora/docker-ce.repo

sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```
</Tab>
<Tab value="Ubuntu">
```bash
sudo apt-get update
sudo apt-get install curl tar ca-certificates
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update

sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```
</Tab>
<Tab value="Debian">
```bash
sudo apt-get update
sudo apt-get install curl tar ca-certificates
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update

sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```
</Tab>
<Tab value="Rocky">
```bash
sudo dnf update
sudo dnf install curl tar
sudo dnf config-manager --add-repo https://download.docker.com/linux/rhel/docker-ce.repo
sudo dnf -y install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```
</Tab>
<Tab value="RHEL">
```bash
sudo yum update
sudo yum install curl tar
sudo dnf -y install dnf-plugins-core
sudo dnf config-manager --add-repo https://download.docker.com/linux/rhel/docker-ce.repo
sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```
</Tab>
<Tab value="Arch">
```bash
sudo pacman -Syu
sudo pacman -S curl tar docker
```
</Tab>
</Tabs>

Enable Docker to start on boot:

```bash
sudo systemctl enable --now docker
```

</Step>

<Step>
### Download the Elytra Executable

Choose the binary for your system architecture:

<Tabs items={["amd64", "arm64"]}>
<Tab value="amd64">
```bash
curl -L -o /usr/local/bin/elytra https://github.com/pyrohost/elytra/releases/latest/download/elytra_linux_amd64
sudo chmod u+x /usr/local/bin/elytra
```
</Tab>
<Tab value="arm64">
```bash
curl -L -o /usr/local/bin/elytra https://github.com/pyrohost/elytra/releases/latest/download/elytra_linux_arm64
sudo chmod u+x /usr/local/bin/elytra
```
</Tab>
</Tabs>
</Step>

<Step>
### Install Rustic (Recommended)

<Callout type='error'>
  Rustic is not required to run Elytra. You do however lose out on deduplicated and encrypted backups. The Pyrodactyl team heavily recommends you install it.
</Callout>

To install rustic, you have the choice of building from source, or downloading the latest pre-built binary. The quickest way is to use the pre-built binaries.

<Tabs items={["amd64", "arm64"]}>
<Tab value="amd64">
```bash
mkdir -p /tmp/rustic-install
curl -L https://github.com/rustic-rs/rustic/releases/download/v0.10.0/rustic-v0.10.0-x86_64-unknown-linux-musl.tar.gz | tar -xz -C /tmp/rustic-install
sudo mv /tmp/rustic-install/rustic /usr/local/bin/
```
</Tab>
<Tab value="arm64">
```bash
mkdir -p /tmp/rustic-install
curl -L https://github.com/rustic-rs/rustic/releases/download/v0.10.0/rustic-v0.10.0-aarch64-unknown-linux-musl.tar.gz | tar -xz -C /tmp/rustic-install
sudo mv /tmp/rustic-install/rustic /usr/local/bin/
```
</Tab>
</Tabs>
</Step>

<Step>
### Create SSL Certificates (Optional)

<Callout type='error'>
  Using SSL is not required to run Elytra, but if you checked **Use SSL Connection** when adding the node to your panel, you will need to do this step. It is recommended for connection security.
</Callout>

Before creating an SSL certificate, ensure you have created an A Record for the FQDN you wish to use for the node, pointed to the IP of the machine on which the node will run.

<Tabs items={["Fedora", "Ubuntu", "Debian", "Rocky", "RHEL", "Arch"]}>
<Tab value="Fedora">
```bash
sudo dnf update -y
sudo dnf install certbot -y
sudo certbot certonly --standalone -d node.pyrodactyl.dev
```
</Tab>
<Tab value="Ubuntu">
```bash
sudo apt update
sudo apt install certbot -y
sudo certbot certonly --standalone -d node.pyrodactyl.dev
```
</Tab>
<Tab value="Debian">
```bash
sudo apt update
sudo apt install certbot -y
sudo certbot certonly --standalone -d node.pyrodactyl.dev
```
</Tab>
<Tab value="Rocky">
```bash
sudo dnf install epel-release -y
sudo dnf install certbot -y
sudo certbot certonly --standalone -d node.pyrodactyl.dev
```
</Tab>
<Tab value="RHEL">
```bash
sudo dnf install epel-release -y
sudo dnf install certbot -y
sudo certbot certonly --standalone -d node.pyrodactyl.dev
```
</Tab>
<Tab value="Arch">
```bash
sudo pacman -Syu
sudo pacman -S certbot
sudo certbot certonly --standalone -d node.pyrodactyl.dev
```
</Tab>
</Tabs>
</Step>

<Step>
### Create the Systemd Service

Create the service file:

```bash
sudo nano /etc/systemd/system/elytra.service
```

Paste the following:

```systemd
[Unit]
Description=Pyrodactyl Elytra Daemon
After=docker.service
Requires=docker.service
PartOf=docker.service

[Service]
User=root
WorkingDirectory=/etc/elytra
LimitNOFILE=4096
PIDFile=/var/run/elytra/daemon.pid
ExecStart=/usr/local/bin/elytra
Restart=on-failure
StartLimitInterval=180
StartLimitBurst=30
RestartSec=5s

[Install]
WantedBy=multi-user.target
```

Save and exit the file.
</Step>

<Step>
### Create a Node in the Panel

Navigate to `https://&lt;Your-Domain.com&gt;/admin` in your browser.

1. Click **Locations** in the sidebar, then **Create New**. Name it whatever you like.
2. Click **Nodes** in the sidebar, then **Create New**.

Fill in the required fields:

<div className="not-prose my-4 space-y-3">

<div className="rounded-lg border border-white/10 bg-white/[0.02] p-4">
  <div className="flex items-center gap-2 mb-1">
    <Icon name="tag" className="size-4 text-blue-400" />
    <span className="font-medium text-sm text-white">Name</span>
  </div>
  <p className="text-sm text-white/50">Give it a unique name that you can identify this node by.</p>
</div>

<div className="rounded-lg border border-white/10 bg-white/[0.02] p-4">
  <div className="flex items-center gap-2 mb-1">
    <Icon name="globe" className="size-4 text-green-400" />
    <span className="font-medium text-sm text-white">Public FQDN</span>
  </div>
  <Callout type="info">This address should be the address that people will use when connecting to your node, not panel to node.</Callout>
  <p className="text-sm text-white/50 mt-2">If your panel is HTTPS-secured, it has to be a domain name. If HTTP, it has to be an IP address.</p>
</div>

<div className="rounded-lg border border-white/10 bg-white/[0.02] p-4">
  <div className="flex items-center gap-2 mb-1">
    <Icon name="memory-stick" className="size-4 text-yellow-400" />
    <span className="font-medium text-sm text-white">Total Memory</span>
  </div>
  <p className="text-sm text-white/50">How much memory your server has.</p>
  <Callout type='warn'>This is not a hard value. The panel will only get mad at you, not stop you!</Callout>
</div>

<div className="rounded-lg border border-white/10 bg-white/[0.02] p-4">
  <div className="flex items-center gap-2 mb-1">
    <Icon name="hard-drive" className="size-4 text-purple-400" />
    <span className="font-medium text-sm text-white">Total Disk Space</span>
  </div>
  <p className="text-sm text-white/50">How much disk space you have available.</p>
  <Callout type='warn'>This is not a hard value. The panel will only get mad at you, not stop you!</Callout>
</div>

</div>

Click **Create Node**. Once it loads, proceed to the Configuration tab.
</Step>

<Step>
### Configure and Start Elytra

On the node page, click the **Configuration** tab, then use the auto-deploy button to generate the setup command. It will look something like:

```bash
cd /etc/elytra && sudo elytra configure --panel-url http://panel.pyrodactyl.dev --token pyro_XWsMcVoJkoMCWn9COtSsUTlbRSevBbLJ5HEMKZQjnN5 --node 1
```

Run the command to configure Elytra.

To test that everything works, run Elytra directly:

```bash
elytra
```

Once confirmed working, enable the service to start on boot:

```bash
sudo systemctl enable --now elytra
```

</Step>
</Steps>

## Troubleshooting

### Failed to Create System User

If Elytra refuses to start with `failed to create pyrodactyl system user error=exit status 4`, manually create the user:

<Tabs items={["Fedora", "Ubuntu", "Debian", "Rocky", "RHEL", "Arch"]}>
<Tab value="Fedora">
```bash
sudo getent passwd pyrodactyl || sudo useradd --system --create-home --shell /usr/sbin/nologin --comment "Elytra/Pterodactyl system user" pyrodactyl
```
Retry with `sudo` if you are prompted for permissions again; the service runs as root before dropping privileges.
</Tab>
<Tab value="Ubuntu">
```bash
sudo getent passwd pyrodactyl || sudo useradd --system --create-home --shell /usr/sbin/nologin --comment "Elytra/Pterodactyl system user" pyrodactyl
```
Ubuntu keeps `nologin` under `/usr/sbin`, matching what the service unit expects.
</Tab>
<Tab value="Debian">
```bash
sudo getent passwd pyrodactyl || sudo useradd --system --create-home --shell /usr/sbin/nologin --comment "Elytra/Pterodactyl system user" pyrodactyl
```
Use the same path as Ubuntu since Debian shares the same filesystem layout for administrative binaries.
</Tab>
<Tab value="Rocky">
```bash
sudo getent passwd pyrodactyl || sudo useradd --system --create-home --shell /usr/sbin/nologin --comment "Elytra/Pterodactyl system user" pyrodactyl
```
Red Hat derivatives keep `nologin` in `/usr/sbin`, so this mirrors the Fedora command exactly.
</Tab>
<Tab value="RHEL">
```bash
sudo getent passwd pyrodactyl || sudo useradd --system --create-home --shell /usr/sbin/nologin --comment "Elytra/Pterodactyl system user" pyrodactyl
```
If SELinux is enforcing, rerun `sudo restorecon /etc/passwd` after creating the user to ensure the context is correct.
</Tab>
<Tab value="Arch">
```bash
sudo getent passwd pyrodactyl || sudo useradd --system --create-home --shell /usr/bin/nologin --comment "Elytra/Pterodactyl system user" pyrodactyl
```
Arch places `nologin` in `/usr/bin`, so point the command there to keep systemd happy.
</Tab>
</Tabs>
